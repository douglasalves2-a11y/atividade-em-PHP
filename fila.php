<?php

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $texto = $_POST["texto"];
    $fila = range(1, $numero);
    echo "Fila: " . implode(", ", $fila);
  }
  if ($_SERVER["
  "] == "") {
    $texto = $_POST["texto"];
    $fila = range(1, $numero);
    echo "
    ". implode(", ", $fila);
  }
?>